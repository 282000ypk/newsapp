Google login steps

1)Craete a WebApplicationClient object by passing google provided client id as parameter.

2) make a request to belwo url and fetch the json that contains gives us the endpoint to which user is to be redirected to get authenticated.
	https://accounts.google.com/.well-known/openid-configuration

	store the value of the key "authorization_endpoint" from above json

3) Prepare request URI using the WebApplicationClient object and authorization_endpointas follows
	request_uri = client.prepare_request_uri(authorization_endpoint, redirect_uri = request.base_url + "/callback", scope = ["openid", "email", "profile"])
	
4) redirect user to the url create in above step

5) google send response to callback url mentioned in credentials and in above request_uri as mentioned

6) 
	